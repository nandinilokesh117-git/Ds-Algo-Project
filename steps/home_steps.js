Given('the user has browser open', async ({}) => {
  // Step: Given the user has browser open
  // From: features/home.feature:7:5
});

When('the user enters the correct DS Algo Portal URL', async ({}) => {
  // Step: When the user enters the correct DS Algo Portal URL
  // From: features/home.feature:10:5
});

Then('the user should be able to land on DS Algo portal with Get Started button', async ({}) => {
  // Step: Then the user should be able to land on DS Algo portal with Get Started button
  // From: features/home.feature:11:5
});

Given('the user is on the DS Algo Portal', async ({}) => {
  // Step: Given the user is on the DS Algo Portal
  // From: features/home.feature:14:5
});

When('the user clicks the {string} button on the landing page', async ({}, arg) => {
  // Step: When the user clicks the "Get Started" button on the landing page
  // From: features/home.feature:15:5
});

Then('the user should be navigated to the Data Structure Introduction page', async ({}) => {
  // Step: Then the user should be navigated to the Data Structure Introduction page
  // From: features/home.feature:16:5
});

Then('the page should display {string} and {string} links', async ({}, arg, arg1) => {
  // Step: And the page should display "Register" and "Sign in" links
  // From: features/home.feature:17:5
});

Given('the user is on the Home page', async ({}) => {
  // Step: Given the user is on the Home page
  // From: features/home.feature:20:5
});

When('the user clicks the {string} dropdown on the Home page', async ({}, arg) => {
  // Step: When the user clicks the "Data Structures" dropdown on the Home page
  // From: features/home.feature:21:5
});

Then('the user should be able to see the following options in the dropdown:', async ({}, dataTable) => {
  // Step: Then the user should be able to see the following options in the dropdown:
  // From: features/home.feature:22:5
});